# Local development only — dev-grade credentials, never used in staging/production.
# Copy to .env and adjust if needed: cp .env.example .env

# --- Postgres ---
POSTGRES_USER=nectrix
POSTGRES_PASSWORD=nectrix
POSTGRES_DB=nectrix
POSTGRES_PORT=5432

# --- Redis ---
REDIS_PORT=6379

# --- Kafka ---
KAFKA_PORT=9092
KAFKA_UI_PORT=8081

# --- MinIO ---
MINIO_ROOT_USER=nectrix
MINIO_ROOT_PASSWORD=nectrix123
MINIO_API_PORT=9000
MINIO_CONSOLE_PORT=9001
