apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
# `kustomize edit set image ghcr.io/avison9/nectrix/<app>:<sha>` (run by CI
# from this directory) appends/updates entries here at deploy time.
images: []
