package com.nectrix.coreapp.trading.service;

/** The invitation hasn't been accepted (or the caller isn't who accepted it) — mapped to 403. */
public class InvitationNotAcceptedException extends RuntimeException {}
