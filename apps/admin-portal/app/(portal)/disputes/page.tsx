import { StubPage } from "@/components/StubPage";

export default function DisputesPage() {
  return (
    <StubPage
      title="Disputes"
      description="Performance-fee ledger dispute resolution — uphold/adjust/void workflows against audited compensating records — Phase 1."
    />
  );
}
