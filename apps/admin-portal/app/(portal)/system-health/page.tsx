import { StubPage } from "@/components/StubPage";

export default function SystemHealthPage() {
  return (
    <StubPage
      title="System Health"
      description="Broker adapter connection counts, message-bus consumer lag, and Copy Engine pipeline throughput/error rate — Phase 1."
    />
  );
}
