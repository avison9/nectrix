// Stub only — no real endpoints exist yet (out of scope for TICKET-001).
// Populated once TICKET-005 (auth) and later tickets expose real HTTP APIs.

import type { NormalizedTradeEvent } from "@nectrix/domain-model";

export type { NormalizedTradeEvent };

export const API_CLIENT_VERSION = "0.1.0";
