# Environment
.env

# Java / Gradle
.gradle/
build/
*.class

# Go
bin/
# `go build` with no -o writes a binary named after the directory in place.
apps/copy-engine/copy-engine
apps/broker-adapters/broker-adapters
apps/mt5-bridge-gateway/mt5-bridge-gateway

# Native JDK/SDK bundles accidentally copied/symlinked into the tree
*.jdk/

# Node
node_modules/
.next/
dist/
.turbo/
*.tsbuildinfo

# Terraform (.terraform.lock.hcl IS committed, for reproducible provider versions)
.terraform/
terraform.tfstate
terraform.tfstate.backup
terraform.tfstate.d/
*.tfplan
deploy/components/local-minio/secret.yaml

# IDE
.idea/
.vscode/*
!.vscode/extensions.json
*.iml

# OS
.DS_Store
